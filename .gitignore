# Dependencies
node_modules/

# Build output
dist/

# Secrets — never commit tokens, keys, or local config
.env
.env.*
*.token
token.txt

# Local-only orientation docs — never published to the public repo
HANDOFF.md
backlog.md

# Design specs are local-only build scaffolding — extract invariants to the code, then delete.
# Kept out of the published repo (history retains any that were committed).
docs/superpowers/specs/

# OS / editor cruft
.DS_Store
Thumbs.db
